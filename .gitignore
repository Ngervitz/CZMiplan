# Optional local overrides with secrets — never deploy
js/config.local.js
config.local.js

# Root-only optional config (do NOT ignore js/config.js or server/config.js)
/config.js

.env
server/.env

# Dependencies (install via npm; Railway/Vercel install on deploy)
node_modules/

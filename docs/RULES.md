INVARIANTS — never break without explicit DEC-XX decision

# ARQUITECTURA

RULE-ARCH-01 | Canonical flow: calcularMotor() → financial_stage → resolveDashboardCoherence() → UI
RULE-ARCH-02 | Motor logic never touches UI directly
RULE-ARCH-03 | narrative_decision owned exclusively by resolveDashboardCoherence()
RULE-ARCH-04 | Primary Action Card is sole owner of next_step guidance
RULE-ARCH-05 | Hero never renders next_step block when Primary Action Card is present
RULE-ARCH-06 | All narrative overrides go through resolveDashboardCoherence() — never scattered in UI components
RULE-ARCH-07 | Presentation-only solutions preferred over motor changes when both achieve the goal

NEVER_TOUCH_WITHOUT_APPROVAL:
  - calcularMotor()
  - interpretarDiagnostico()
  - resolveDashboardCoherence()
  - resolveContextualActionSegment()
  - seleccionarAccionesRecomendadas()
  - CRM payloads
  - GTM / GA4 / Meta tracking
  - action_ids
  - B7 schema
  - Entry Context Layer (window.CZ_ENTRY_CONTEXT)

# CAPAS ORTOGONALES

RULE-LAYERS-01 | financial_stage ⊥ user_intent ⊥ entry_context — never conflate
RULE-LAYERS-02 | entry_context NEVER forces or modifies financial_stage
RULE-LAYERS-03 | CDV users ALWAYS bypass P11 (user intent question)
RULE-LAYERS-04 | ORDENAR is a learning_mode flag, not a user_intent
RULE-LAYERS-05 | Rejection copy ONLY when CZ_ENTRY_CONTEXT.hasRejectionContext === true

FINANCIAL_STAGES: CLARIDAD | RECUPERACIÓN | ESTABILIZACIÓN | OPTIMIZACIÓN
USER_INTENTS: RECUPERAR | CREDITO | OPTIMIZAR
ENTRY_CONTEXTS: RECHAZO_CDV | RECHAZO_GENERAL | SEO_IA | ORGANICO
NARRATIVE_MODES: RECOVERY | STABILIZATION | OPTIMIZATION | CLARITY

# CONFLICTO DATOS vs INTENT

RULE-CONFLICT-01 | Datos siempre ganan sobre intent declarado
  EXAMPLE: user wants OPTIMIZAR but data shows RECUPERACIÓN → system names the conflict
  COPY: "Tu objetivo es optimizar, pero hoy existe una situación financiera que conviene resolver primero."

# NARRATIVA Y COPY

RULE-COPY-01 | System is a thermometer, not a coach — never coaching language
RULE-COPY-02 | No aspirational language
RULE-COPY-03 | No unsupported optimism
RULE-COPY-04 | No advice not justified by profile data
RULE-COPY-05 | No generic financial education unless directly supported by diagnosis
RULE-COPY-06 | No "para que el banco te diga que sí" or similar approval promises
RULE-COPY-07 | Narrative framing = discovery ("usuario gana visibilidad"), never transformation ("vida mejora")
RULE-COPY-08 | Plan titles = plan objective, not diagnosis or stage

# PLUS

RULE-PLUS-01 | alignment_label generated by reconciliation engine, NEVER by LLM
RULE-PLUS-02 | Claude API key NEVER in browser code — always proxied via Vercel serverless
RULE-PLUS-03 | _fetchPlusReportProvider() is the only entry point to Plus report generation
RULE-PLUS-04 | Degraded mode must be active when external data (Equifax, Handy) unavailable

# STACK

RULE-STACK-01 | Vanilla JS only — no frameworks, no npm, no build step
RULE-STACK-02 | config.js ALWAYS in .gitignore
RULE-STACK-03 | API keys NEVER in browser code
RULE-STACK-04 | source=seo_ia routes to SEO IA onboarding flow

# DATOS Y EVIDENCIA

RULE-DATA-01 | No features without real user data — default is Camino B (no change)
RULE-DATA-02 | Visual observations alone do not justify structural redesign
RULE-DATA-03 | Scroll depth + interaction + conversion metrics required before dashboard restructuring
RULE-DATA-04 | Every issue classified before fixing: Technical Bug | Narrative Issue | UX Issue | Copy Issue

# QA

RULE-QA-01 | SyntheticMotorQA (31/31) must pass before any motor changes merge
RULE-QA-02 | dashboard-e2e-qa must pass before any UI changes merge
RULE-QA-03 | Production fixes require QA validation before acceptance
RULE-QA-04 | Commit deployment does NOT equal production verification
RULE-QA-05 | Pending production observations remain unresolved until visually confirmed

# GTM / TRACKING

RULE-GTM-01 | GTM payloads: safe only — no plan_id, no score, no financial data
RULE-GTM-02 | CRM payloads: full context permitted
RULE-GTM-03 | income_updated and profile_extreme_debt events: CRM only, NOT GTM, NOT Meta

# LEGAL / COMPLIANCE

RULE-LEGAL-01 | Score is orientativo — never described as crediticio
RULE-LEGAL-02 | No promise of credit approval in any copy
RULE-LEGAL-03 | Diagnóstico framed as informativo, not determinante
RULE-LEGAL-04 | Data protection: Ley 18.331 compliance required
RULE-LEGAL-05 | Consumer protection: Ley 17.250 compliance required
RULE-LEGAL-06 | URCDP registration: completed — maintain compliance

# SAAS / MULTI-TENANCY

RULE-SAAS-01 | No hardcoded client-specific logic
RULE-SAAS-02 | All parameters must be configurable
RULE-SAAS-03 | Every new feature evaluated for future SaaS reusability
RULE-SAAS-04 | Architecture decisions must not close door to white-label productization

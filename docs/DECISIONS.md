FORMAT: DEC-ID | DECISION | REASON | STATUS

# ARQUITECTURA CORE

DEC-MOTOR | calcularMotor() is sacred — never touch without explicit approval
  REASON: single source of financial truth, all scoring depends on it
  NEVER_TOUCH: calcularMotor() | interpretarDiagnostico() | resolveDashboardCoherence() | resolveContextualActionSegment() | seleccionarAccionesRecomendadas() | CRM | GTM | GA4 | Meta | tracking | action_ids | B7 schema | Entry Context Layer
  STATUS: ACTIVE

DEC-FLOW | Canonical architecture flow is non-negotiable
  DECISION: calcularMotor() → financial_stage → resolveDashboardCoherence() → UI
  REASON: clean separation of concerns, prevents motor logic leaking into UI
  STATUS: ACTIVE

DEC-LAYERS | Three orthogonal layers must never conflate
  LAYERS: financial_stage ⊥ user_intent ⊥ entry_context
  REASON: each layer answers a different question; mixing them corrupts narrative logic
  STATUS: ACTIVE

DEC-COHERENCE | resolveDashboardCoherence() is the narrative orchestrator
  DECISION: all narrative overrides go through resolveDashboardCoherence(), not individual components
  REASON: prevents scattered narrative hacks across UI components
  OUTPUTS: profileTier | nextStepKey | nextStepText | heroProblemOverride | showRetry | hideAccionPrioritaria | narrativeOverride
  STATUS: ACTIVE

# NARRATIVA Y COPY

DEC-DIAGNOSTIC | Mi Plan is a diagnostic product, not a coach
  DECISION: system behaves as a thermometer, not a coach
  REASON: coaching language reduces trust, creates unsupported recommendations
  PROHIBITS: aspirational language | unsupported optimism | generic financial education | advice not justified by data
  REQUIRES: observable facts | observable consequences | concrete language
  STATUS: ACTIVE

DEC-NARRATIVE-DISCOVERY | Narrative framing is discovery, not transformation
  DECISION: "usuario gana visibilidad" — never "vida mejora" or "recuperación garantizada"
  REASON: legally safer for regulated financial products; more honest
  STATUS: ACTIVE

DEC-COPY-REALISTIC | Realistic optimism only
  DECISION: eliminated phrases like "Eso marca la diferencia", "La buena noticia", "para que el banco te diga que sí"
  IMPLEMENTED: COPY-2B
  EXCEPTION: "En buen camino" retained | "Plan 3 objetivo" retained
  STATUS: ACTIVE

DEC-REJECTION-ASSUMPTION | Never assume user came from a credit rejection
  DECISION: rejection copy only shown when CZ_ENTRY_CONTEXT.hasRejectionContext === true
  REASON: Mi Plan now receives organic, SEO, ecommerce, insurance, and Plus users — not only rejected applicants
  IMPLEMENTED: FIX-01B-1
  STATUS: ACTIVE

DEC-PLAN-TITLES | Plan titles represent the plan objective, not a diagnosis
  DECISION: titles = objetivo del plan, not etapa or status
  EXAMPLES: Plan 1 → Claridad Financiera | Plan 5 → Reconstrucción Crediticia
  IMPLEMENTED: FIX-01B-2
  STATUS: ACTIVE

# ENTRY CONTEXT

DEC-04D | entry_context NEVER forces or modifies financial_stage
  DECISION: RECHAZO_CDV does not imply debt, mora, or financial pressure
  VALID_CASE: RECHAZO_CDV + OPTIMIZACIÓN is a valid combination
  EXAMPLE_NARRATIVE: "No vemos presión financiera significativa. El rechazo puede estar relacionado con factores fuera del alcance de este diagnóstico."
  may_influence: narrative | hero | copy | focus
  may_NOT_influence: financial_stage
  REASON: prevents diagnostic manipulation based on acquisition channel
  STATUS: ACTIVE

DEC-CDV-BYPASS | CDV users bypass P11 (user intent question)
  REASON: intent is implicit (CREDITO) — no need to ask
  IMPLEMENTED: active
  STATUS: ACTIVE

# NEXT STEP OWNERSHIP

DEC-NEXT-STEP | Primary Action Card is sole owner of next_step guidance
  DECISION: Hero suppresses its next-step block when Primary Action Card renders
  REASON: duplicate guidance caused UX confusion
  IMPLEMENTED: UX-CONSOLIDATION-01-FIX (commit 3b0f764)
  STATUS: ACTIVE

DEC-SINGLE-ACTION | Single Action Dashboard approved
  DECISION: Nivel 1 only (not Nivel 2 or Nivel 3)
  STATUS: ACTIVE

# PLUS

DEC-PLUS-REPOSITION | Mi Plan Plus repositioned as "auditor de realidad financiera"
  KPI: alignment_label (Alta/Parcial/Baja coincidencia)
  RULE: alignment_label generated by reconciliation engine, NOT by LLM
  STATUS: ACTIVE

DEC-PLUS-PROVIDER | _fetchPlusReportProvider() as backend abstraction
  REASON: enables future migration to /api/plus/generate without changing consumers
  STATUS: ACTIVE

DEC-PLUS-DEFERRED | Plus card placement and repositioning deferred
  REASON: no real user evidence on conversion impact
  REQUIRED_BEFORE_REOPEN: real users | scroll depth | conversion data | interaction data
  STATUS: DEFERRED

# DATA AND EVIDENCE

DEC-DATA-FIRST | No features without real user data
  APPLIES_TO: Plus card placement | INSIGHTS-01 | A/B architecture | dashboard restructuring
  DEFAULT: Camino B (no change until data exists)
  STATUS: ACTIVE

DEC-AUDIT-FIRST | Audit before implementation — always
  SEQUENCE: audit → diagnose → design → implement
  REASON: historical tendency toward premature feature creation
  APPLIES_TO: all sprints touching motor | scoring | resolver
  STATUS: ACTIVE

# SAAS / PRODUCTIZACIÓN

DEC-SAAS | All architecture must preserve white-label B2B path
  DECISION: no hardcoded client-specific logic | all parameters configurable | multi-tenancy by design
  RULE: every new feature must be evaluated for future SaaS reusability
  STATUS: ACTIVE, not in active sprint

# ZERO ACTIVE DEBT

DEC-ZERO-DEBT | Special case: zero active debt with complete profile
  TRIGGER: ingresos completos + gastos completos + deuda cancelada + 0 deudas activas + Plan 1 guardrail
  COPY_problema: "No registrás deudas activas actualmente. El objetivo es mantener un equilibrio saludable entre ingresos y gastos para conservar tu estabilidad financiera."
  COPY_proximo_paso: "Revisá periódicamente tus gastos y tu margen disponible para mantener una situación financiera saludable."
  HELPER: _isZeroActiveDebtCompleteProfile()
  APPLIES_TO: Hero | Acción prioritaria | Narrativa | Próximo paso
  IMPLEMENTED: B2e
  STATUS: ACTIVE

# SCORE VISIBILITY

DEC-SCORES-HIDDEN | Internal scores not shown to users
  DECISION: scoreReset | scoreFinanciero | scoreConductual replaced by human-readable labels
  LABELS_financial: Crítica | Presión financiera | En recuperación | Estable
  LABELS_conductual: Necesita trabajo | En desarrollo | Consistente | Sólido
  NOTE: scores remain active internally and in CRM
  STATUS: ACTIVE
